ml100k_path: "data/ml100k_ratings.csv"
sep: ";"
snapshot_gran: "d"
window_sids: 0

node_dim: 64
embed_dim: 64
compressed_dim: 16

n_layers: 2
dropout: 0.1

lr: 0.0003
weight_decay: 0.00001
epochs: 50

train_ratio: 0.7
val_ratio: 0.15
test_ratio: 0.15

k: 20

seed: 1337
device: "cuda"

debug_sids: 3
full_train_epochs: 30

distillation_weight: 0.1
grad_clip: 1.0

use_scheduler: false
use_grad_checkpointing: true
grad_accum_steps: 4

project: "dynamic_gnn_recommender_movielens"
run_name: "gcn_L2_emb64_comp16_zero1_grad_accum_x4_checkpointing"
checkpoint_dir: "checkpoints"

devices: 2
num_nodes: 1

zero_stage: 1
zero_offload_optimizer: false
zero_offload_optimizer_device: "cpu"
