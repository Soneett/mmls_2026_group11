ml100k_path: "data/ml100k_ratings.csv"
sep: ";"
snapshot_gran: "d"
window_sids: 0

node_dim: 64
embed_dim: 64
compressed_dim: 16

n_layers: 2
dropout: 0.1

lr: 0.0003
weight_decay: 0.00001
epochs: 50

train_ratio: 0.7
val_ratio: 0.15
test_ratio: 0.15

k: 20

seed: 1337
device: "cuda"

debug_sids: 0
full_train_epochs: 50

distillation_weight: 0.0
grad_clip: 1.0

use_scheduler: true
use_grad_checkpointing: false

project: "dynamic_gnn_recommender_movielens"
run_name: "student_ce_L2_emb64_comp16_kaggle"
checkpoint_dir: "checkpoints"
use_wandb: false
checkpoint_filename: "best"

training_objective: "student_ce"
kd_student_branch: "small"
